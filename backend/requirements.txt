# ── API server ────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9          # file upload support

# ── Transcription ─────────────────────────────────────────────
openai-whisper==20231117         # local Whisper model

# ── RAG pipeline ──────────────────────────────────────────────
langchain==0.2.0
langchain-community==0.2.0
langchain-openai==0.1.6          # swap for langchain-ollama if running locally
openai==1.30.1                   # LLM backend (GPT-4o / GPT-3.5)

# ── Vector database ───────────────────────────────────────────
chromadb==0.5.0

# ── Embeddings ────────────────────────────────────────────────
sentence-transformers==3.0.0     # local embeddings (all-MiniLM-L6-v2)
# torch is a transitive dep of sentence-transformers.
# Install CPU-only build to avoid a 2 GB CUDA download on machines without GPU:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# (sentence-transformers will still work correctly on CPU)
numpy>=1.24,<2.0                 # required by sentence-transformers + chromadb

# ── Relational / session storage ──────────────────────────────
aiosqlite==0.20.0                # async SQLite driver
sqlalchemy==2.0.30               # ORM / query builder

# ── Utilities ─────────────────────────────────────────────────
python-dotenv==1.0.1             # .env config loading
pydantic==2.7.1                  # data validation (bundled with FastAPI)
pydantic-settings==2.2.1         # settings management
httpx==0.27.0                    # async HTTP client (testing / proxying)
